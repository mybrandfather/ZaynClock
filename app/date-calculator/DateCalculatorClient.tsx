'use client'
import {useMemo,useState} from 'react'
function parse(v:string){const [y,m,d]=v.split('-').map(Number);return new Date(y,m-1,d)}
export default function DateCalculatorClient(){const today=new Date().toISOString().slice(0,10);const [start,setStart]=useState(today);const [amount,setAmount]=useState(30);const [unit,setUnit]=useState<'days'|'weeks'|'months'|'years'>('days');const [direction,setDirection]=useState<1|-1>(1);const result=useMemo(()=>{const d=parse(start);if(unit==='days')d.setDate(d.getDate()+direction*amount);if(unit==='weeks')d.setDate(d.getDate()+direction*amount*7);if(unit==='months')d.setMonth(d.getMonth()+direction*amount);if(unit==='years')d.setFullYear(d.getFullYear()+direction*amount);return d},[start,amount,unit,direction]);return <div className="card" style={{maxWidth:820,margin:'0 auto'}}><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(170px,1fr))',gap:'1rem'}}><label style={label}>Start date<input style={input} type="date" value={start} onChange={e=>setStart(e.target.value)}/></label><label style={label}>Add or subtract<select style={input} value={direction} onChange={e=>setDirection(Number(e.target.value) as 1|-1)}><option value={1}>Add</option><option value={-1}>Subtract</option></select></label><label style={label}>Amount<input style={input} type="number" min={0} max={10000} value={amount} onChange={e=>setAmount(Math.max(0,Number(e.target.value)||0))}/></label><label style={label}>Unit<select style={input} value={unit} onChange={e=>setUnit(e.target.value as typeof unit)}><option value="days">Days</option><option value="weeks">Weeks</option><option value="months">Months</option><option value="years">Years</option></select></label></div><div aria-live="polite" style={{marginTop:'1.3rem',padding:'1.2rem',textAlign:'center',borderRadius:12,background:'var(--bg-secondary)',border:'1px solid var(--border)'}}><div style={{fontSize:'.78rem',color:'var(--text-secondary)',textTransform:'uppercase'}}>Result date</div><strong style={{display:'block',fontSize:'clamp(1.4rem,4vw,2.1rem)',color:'var(--accent)',marginTop:'.3rem'}}>{new Intl.DateTimeFormat('en-US',{weekday:'long',year:'numeric',month:'long',day:'numeric'}).format(result)}</strong></div></div>}
const label={display:'grid',gap:'.45rem',color:'var(--text-secondary)',fontSize:'.88rem'} as const;const input={padding:'.75rem',borderRadius:8,border:'1px solid var(--border)',background:'var(--bg-primary)',color:'var(--text-primary)',colorScheme:'dark'} as const
