'use client'
import{useMemo,useState}from'react'
function isoWeek(d:Date){const x=new Date(Date.UTC(d.getFullYear(),d.getMonth(),d.getDate()));const day=x.getUTCDay()||7;x.setUTCDate(x.getUTCDate()+4-day);const yearStart=new Date(Date.UTC(x.getUTCFullYear(),0,1));return{week:Math.ceil((((x.getTime()-yearStart.getTime())/86400000)+1)/7),year:x.getUTCFullYear(),day:day}}
export default function WeekNumberClient(){const [value,setValue]=useState(new Date().toISOString().slice(0,10));const result=useMemo(()=>{const [y,m,d]=value.split('-').map(Number);return isoWeek(new Date(y,m-1,d))},[value]);return <div className="card" style={{maxWidth:680,margin:'0 auto'}}><label style={{display:'grid',gap:'.45rem',color:'var(--text-secondary)'}}>Choose a date<input type="date" value={value} onChange={e=>setValue(e.target.value)} style={{padding:'.8rem',borderRadius:8,border:'1px solid var(--border)',background:'var(--bg-primary)',color:'var(--text-primary)',colorScheme:'dark'}}/></label><div style={{textAlign:'center',padding:'1.5rem',marginTop:'1rem',background:'var(--bg-secondary)',border:'1px solid var(--border)',borderRadius:12}}><div style={{fontSize:'.78rem',color:'var(--text-secondary)'}}>ISO WEEK NUMBER</div><strong style={{display:'block',fontSize:'3rem',color:'var(--accent)'}}>{result.week}</strong><p>ISO week-year {result.year} · Day {result.day} of the week</p></div></div>}
