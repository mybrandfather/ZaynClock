import type { Metadata } from 'next'
import AgeCalculatorClient from './AgeCalculatorClient'
import JsonLd,{SITE_URL,breadcrumbSchema,softwareAppSchema} from '@/components/seo/JsonLd'
export const metadata:Metadata={title:'Age Calculator – Exact Age in Years, Months and Days',description:'Calculate exact age in years, months and days, total days, weeks and the countdown to the next birthday.',alternates:{canonical:`${SITE_URL}/age-calculator`}}
export default function Page(){return <main style={{maxWidth:980,margin:'0 auto',padding:'1.5rem'}}><JsonLd data={[softwareAppSchema({name:'ZaynClock Age Calculator',description:'Calculate exact age and birthday countdown.',url:'/age-calculator'}),breadcrumbSchema([{name:'Home',url:'/'},{name:'Tools',url:'/tools'},{name:'Age Calculator',url:'/age-calculator'}]) ]}/><header style={{textAlign:'center',marginBottom:'1.5rem'}}><h1>Age Calculator</h1><p style={{color:'var(--text-secondary)'}}>Find an exact age and the number of days until the next birthday.</p></header><AgeCalculatorClient/><section style={{maxWidth:820,margin:'2rem auto',lineHeight:1.8,color:'var(--text-secondary)'}}><h2 style={{color:'var(--text-primary)'}}>How age is calculated</h2><p>The calculator counts completed calendar years first, then completed months, then remaining days. This avoids the inaccuracies caused by treating every month as the same length.</p></section></main>}
