'use client'
import{useEffect,useMemo,useState}from'react'
export default function DateCountdownClient(){const tomorrow=new Date();tomorrow.setDate(tomorrow.getDate()+30);const [title,setTitle]=useState('My event');const [target,setTarget]=useState(tomorrow.toISOString().slice(0,16));const [now,setNow]=useState(Date.now());useEffect(()=>{const id=window.setInterval(()=>setNow(Date.now()),1000);return()=>clearInterval(id)},[]);const r=useMemo(()=>{let ms=new Date(target).getTime()-now;const past=ms<0;ms=Math.abs(ms);const days=Math.floor(ms/86400000);const hours=Math.floor(ms/3600000)%24;const minutes=Math.floor(ms/60000)%60;const seconds=Math.floor(ms/1000)%60;return{past,days,hours,minutes,seconds}},[target,now]);return <div className="card" style={{maxWidth:820,margin:'0 auto'}}><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(220px,1fr))',gap:'1rem'}}><label style={label}>Event name<input style={input} value={title} maxLength={80} onChange={e=>setTitle(e.target.value)}/></label><label style={label}>Date and time<input style={input} type="datetime-local" value={target} onChange={e=>setTarget(e.target.value)}/></label></div><div aria-live="polite" style={{marginTop:'1.4rem',textAlign:'center'}}><h2>{title||'Your event'} {r.past?'was':'is in'}</h2><div style={{display:'grid',gridTemplateColumns:'repeat(4,1fr)',gap:'.65rem',marginTop:'1rem'}}>{[['Days',r.days],['Hours',r.hours],['Minutes',r.minutes],['Seconds',r.seconds]].map(([l,v])=><div key={String(l)} style={{padding:'1rem .4rem',background:'var(--bg-secondary)',border:'1px solid var(--border)',borderRadius:10}}><strong style={{fontSize:'clamp(1.4rem,5vw,2rem)',color:'var(--accent)'}}>{v}</strong><div style={{fontSize:'.72rem',color:'var(--text-secondary)'}}>{l}</div></div>)}</div></div></div>}
const label={display:'grid',gap:'.45rem',color:'var(--text-secondary)',fontSize:'.88rem'} as const;const input={padding:'.75rem',borderRadius:8,border:'1px solid var(--border)',background:'var(--bg-primary)',color:'var(--text-primary)',colorScheme:'dark'} as const
