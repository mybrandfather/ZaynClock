import type{Metadata}from'next';import DateCountdownClient from './DateCountdownClient';import JsonLd,{SITE_URL,breadcrumbSchema,softwareAppSchema}from'@/components/seo/JsonLd'
export const metadata:Metadata={title:'Date Countdown – Days Until Any Event',description:'Create a live countdown to a birthday, exam, holiday, trip, wedding or any future date and time.',alternates:{canonical:`${SITE_URL}/date-countdown`}}
export default function Page(){return <main style={{maxWidth:980,margin:'0 auto',padding:'1.5rem'}}><JsonLd data={[softwareAppSchema({name:'ZaynClock Date Countdown',description:'Live countdown to any date and time.',url:'/date-countdown'}),breadcrumbSchema([{name:'Home',url:'/'},{name:'Tools',url:'/tools'},{name:'Date Countdown',url:'/date-countdown'}])]}/><header style={{textAlign:'center',marginBottom:'1.5rem'}}><h1>Date Countdown</h1><p style={{color:'var(--text-secondary)'}}>Count down to any birthday, holiday, exam, trip or event.</p></header><DateCountdownClient/></main>}
